<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Используем стандартную тему Android, чтобы не требовать MaterialComponents -->
    <style name="Theme.PizzaApp" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:windowBackground">@color/background_dark</item>
    </style>
</resources>
